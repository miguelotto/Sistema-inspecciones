// Alternar la navegación lateral
/* $("#sidenavToggler").click(function(e) {
  e.preventDefault();
  $("body").toggleClass("sidenav-toggled");
  $(".navbar-sidenav").toggleClass("sidenav-not-toggled");
  $(".navbar-sidenav .nav-link-collapse").addClass("collapsed");
  $(".navbar-sidenav .nav-link-collapseLink").addClass("collapsed");
  $(".navbar-sidenav .sidenav-second-level, .navbar-sidenav .sidenav-third-level").removeClass("show");
});

 */





/* window.addEventListener('DOMContentLoaded', function () {
    const aprobado = document.getElementById('aprobado');
    const reprobado = document.getElementById('reprobado');
  
    if (aprobado) {
      aprobado.addEventListener('change', function () {
        if (aprobado.checked) {
          aprobado.classList.add('active');
          reprobado.classList.remove('active');
        }
      });
    }
  
    if (reprobado) {
      reprobado.addEventListener('change', function () {
        if (reprobado.checked) {
          reprobado.classList.add('active');
          aprobado.classList.remove('active');
        }
      });
    }
  });

   */

/* 
  new TomSelect("#select-beast",{
    create: true,
    sortField: {
      field: "text",
      direction: "asc"
    }
  }); */